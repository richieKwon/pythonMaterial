class DataProcess:
    
    
    FYstart = if datetime.now().month >= 4:
     datetime.date(datetime.now().year, datetime.now().month,1)  else 
    FYend = 
    FY24months
    def __init__(self, productName):
        self.product = productName

